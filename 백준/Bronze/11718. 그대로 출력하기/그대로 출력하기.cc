#include <bits/stdc++.h>
using namespace std;


int main() {
	ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	
	string a;

	while (true) {
		if (cin.eof()) break;
		getline(cin, a);
		cout << a << '\n';
	}

	return 0;
}